![[Pasted image 20260522180925.png]]
// xor everynumber and return the last element in the array
- xor the same number even times returns the  zero
- xor the same number odd number of times same it will return the same number 
- a xor a = 0  || a xor a xor a  = a
- 0 xor b = b

### swap two numbers
- a xor b = c 
- c xor a = b    ya  c xor b = a 
- use this to swap two numbers
